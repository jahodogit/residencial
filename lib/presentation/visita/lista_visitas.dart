import 'package:flutter/material.dart';

class ListaVisitasPage extends StatelessWidget {
  const ListaVisitasPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Visitas ingresadas"),
      ),
      body: Text("Body"),
    );
  }
}
